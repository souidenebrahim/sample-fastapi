[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
httpx = "*"
httpcore= "*"
fastapi = "*"
uvicorn = "*"

[dev-packages]

[scripts]
dev = "uvicorn app.main:app --host 0.0.0.0 --port 3050 --reload --reload-delay 2 --log-level debug"
start = "uvicorn app.main:app --host 0.0.0.0 --port 3050"
format = "black . && isort ."
lint = "flake8 ."

[requires]
python_version = "3.12"
